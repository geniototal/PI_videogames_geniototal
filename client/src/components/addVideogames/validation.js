export const isNombre = ( value ) => {
    if (typeof value === "string") {
        return true    
    } else {
        return false
    }
     
}
export const isRating = ( value ) => {
    if (typeof value === "number") {
        return true    
    } else {
        return false
    }
     
}